{
  "name": "bloom-backend",
  "version": "1.0.0",
  "description": "Backend API for Bloom (Todo/Habit/Mood tracker) — handles server-side logic that shouldn't live in the frontend: aggregated stats, auth-protected endpoints, and admin-level Supabase operations.",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
